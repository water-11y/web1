{
  "name": "web1-shell-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Web1 shell app config platform with Railway PostgreSQL support.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
